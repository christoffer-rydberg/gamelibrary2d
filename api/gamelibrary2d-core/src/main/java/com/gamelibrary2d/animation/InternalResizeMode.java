package com.gamelibrary2d.animation;

enum InternalResizeMode {

    /**
     * Image is resized to the specified bounds.
     */
    ABSOLUTE,

    /**
     * Image is resized using the specified bounds as a scale factor applied to the original image width and height.
     */
    SCALE
}
