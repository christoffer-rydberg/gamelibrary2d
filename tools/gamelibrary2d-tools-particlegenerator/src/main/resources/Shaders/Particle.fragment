#version 130

in vec4 color;
in vec2 vertexCoordinate;

out vec4 fragColor;

uniform sampler2D texImage;

void main() {
 	float dist = distance(vertexCoordinate.xy, vec2(0.5, 0.5));
    fragColor = vec4(color[0], color[1], color[2], color[3] * smoothstep(0.5, 0, dist));
}